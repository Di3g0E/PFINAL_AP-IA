# ============================================================================
# P6_AP-IA — Sistema unificado multiagente (Orquestador + Security + Registrar + Analyst)
# Consolida funcionalidades de P1+P2+P3+P4+P5 en un único entorno.
#
# Anclajes críticos (no relajar sin retest):
#   - numpy<2.0          ← exigido por paddlepaddle 2.6.2 (P3)
#   - scikit-learn==1.5  ← compat con paddleocr y carga de modelos joblib (P3)
#   - urllib3<2          ← compat paddle/requests
#
# Decisiones de simplificación respecto a P1-P5:
#   - Sin transformers/accelerate (P2 producción usa SGDClassifier)
#   - Sin deepface/tf-keras (FaceNet vía facenet-pytorch evita TensorFlow)
#   - Sin easyocr (PaddleOCR como motor único)
#   - Sin pmdarima (statsmodels cubre ARIMA con menos fricción de build)
# ============================================================================

# --- Numerics & ML core (anclados por PaddleOCR) ---
numpy==1.26.4
pandas==2.2.3
scipy>=1.11.0,<1.14
scikit-learn==1.5.0
joblib>=1.3.0
urllib3<2.0.0

# --- Web framework + auth (P6) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# --- Database (P6) ---
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0
alembic>=1.14.0

# --- LangGraph + LLM providers (núcleo del sistema multiagente) ---
langgraph>=0.2.50
langgraph-checkpoint-postgres>=2.0.0
langchain-core>=0.3.20
langchain-groq>=0.2.0
langchain-openai>=0.2.10
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0

# --- OCR — Agente Registrador (P3) ---
paddlepaddle==2.6.2
paddleocr==2.8.1
opencv-python>=4.8.0
Pillow>=10.0.0

# --- Computer Vision / Biometría — Agente Security (P5) ---
torch>=2.1.0,<2.6
torchvision>=0.16.0
facenet-pytorch>=2.5.3
scikit-image>=0.21.0

# --- Time series — Agente Analyst (P1) ---
statsmodels>=0.14.0

# --- NLP — Agente Registrador (P2) ---
nltk>=3.9.0

# --- Cryptography (cifrado de embeddings + API keys de usuario) ---
cryptography>=41.0.0

# --- Observability (Fase 6: monitorización humana con Langfuse) ---
# Si LANGFUSE_* no está configurado, los handlers son no-op y nada falla.
langfuse>=2.50.0,<3.0.0

# --- Notifications (Telegram + WhatsApp opcional) ---
pywhatkit>=5.4
requests>=2.31.0

# --- Utilities ---
python-dotenv>=1.0.0
PyYAML>=6.0
loguru>=0.7.0
tqdm>=4.66.0

# --- Dev / Testing ---
pytest>=7.4.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
